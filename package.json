{
  "name": "metapavo",
  "private": true,
  "scripts": {
    "build:extension": "npm run build:extension:pavo&&npm run build:extension:dashboard",
    "dev:extension": "npm run dev:extension:pavo&&npm run build:extension:dashboard",
    "build:extension:pavo": "lerna run build:extension --stream",
    "dev:extension:pavo": "lerna run build:extension:dev --stream",
    "dev:website": "lerna run dev:website --stream",
    "build:website": "lerna run build:website --stream",
    "build:extension:dashboard": "lerna run build:extension:dashboard --stream",
    "dev:extension:dashboard": "lerna run dev:extension:dashboard --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "@mui/icons-material": "^5.10.9",
    "web3": "^1.8.1"
  }
  
}
