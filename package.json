{
  "name": "metapavo",
  "private": true,
  "scripts": {
    "build:extension": "lerna run build:extension",
    "build:extension:dev": "lerna run build:extension:dev",
    "dev:website": "lerna run dev:website",
    "build:website": "lerna run build:website"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
